# task-openapi-docs-nestjs — Progress

**Status:** completed
**SIs:** 5/5 completed

### SI-1 — Instalar `@nestjs/swagger` + configurar CLI plugin
- **Status:** completed
- **Tests:** no tests
- **Observations:**
  - AC "npm run build emits metadata.ts ao lado de dist/" does not apply to the standard tsc/nest build path — the CLI plugin (TypeScript AST transformer) injects metadata directly into compiled JS (`__OPENAPI_METADATA_FACTORY__()` methods), not into a separate file. `metadata.ts` is only produced by `PluginMetadataGenerator` (SWC path). The other two ACs pass: `npx tsc --noEmit` exits 0 and `node -e "require('@nestjs/swagger')"` loads without error.

### SI-2 — Configuração `swagger.config.ts` + flag `SWAGGER_ENABLED`
- **Status:** completed
- **Tests:** 7 passing
- **Observations:** none

### SI-3 — Montar Swagger UI runtime condicional em `main.ts`
- **Status:** completed
- **Tests:** 6 passing
- **Observations:**
  - Created `src/metadata.ts` stub satisfying the `import('./metadata')` call in `main.ts` — the TypeScript AST transformer (tsc path) injects `__OPENAPI_METADATA_FACTORY__` methods directly into DTOs at compile time; `metadata.ts` is only generated by `PluginMetadataGenerator` (SWC path). The stub is a no-op async function that keeps `tsc --noEmit` clean.
  - `@nestjs/swagger` serves YAML with `Content-Type: text/yaml` (RFC 9512), not `application/yaml`; updated E2E assertion to match `/yaml/`.

### SI-4 — Script `openapi:export` + artefato `openapi.json`
- **Status:** completed
- **Tests:** 5 passing
- **Observations:**
  - Reused `buildSwaggerConfig()` from `src/swagger/swagger-document.ts` to keep DocumentBuilder config DRY between `main.ts` and `openapi-export.ts`.
  - `exportSpec` accepts an optional `outputPath` parameter (defaults to `'openapi.json'`) so the integration test can write to a temp file without touching the project root.
  - `openapi.json` written to `nestjs-project/openapi.json` via first script run; file is ready to be versioned.

### SI-5 — Enriquecer spec OpenAPI com decoradores explícitos nos controllers/DTOs existentes
- **Status:** completed
- **Tests:** 9 passing
- **Observations:**
  - No users controller exists in the codebase — only `auth.controller.ts` was annotated.
  - `buildSwaggerDocument(app)` helper extracted to `swagger-document.ts`; both `main.ts` and `openapi-export.ts` updated to use it, keeping `extraModels: [ApiErrorEnvelope]` in one place.
  - `openapi.json` regenerated with 9 summaries, `ApiErrorEnvelope` in `components.schemas`, and security on `logout`/`me` endpoints.
